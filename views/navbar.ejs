<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/laptimes/<%= user._id%>">Laptime tracker</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link <% if (active === "times") {%>active<% }%>" aria-current="page" href="/laptimes/<%= user._id%>">Times</a>
            </li>
            <li class="nav-item">
                <a class="nav-link <% if (active === "new") {%>active<% }%>" href="/laptimes/<%= user._id%>/newtime">New time</a>
            </li>
        </ul>
        <a href="/logout">
            <button class="btn btn-outline-danger" type="button">Log out</button>
        </a>
    </div>
</nav>